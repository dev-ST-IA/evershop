"""
The `Thresholds` type represents a product's numbers to display in the UI.
"""
type SalesCount {
  salesCount:Int
  salesCountThres: Int!
  canShowSalesCount: Boolean
}
 
extend type Product {
  saleCountThreshold: SalesCount!
}
